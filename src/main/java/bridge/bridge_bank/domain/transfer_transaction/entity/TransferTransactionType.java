package bridge.bridge_bank.domain.transfer_transaction.entity;

public enum TransferTransactionType {
    SIMPLE_TRANSFER_IN,
    RESERVE_ONCE_IN,
    RESERVE_REPEAT_IN,
    SIMPLE_TRANSFER_OUT,
    RESERVE_ONCE_OUT,
    RESERVE_REPEAT_OUT
}
